language: cpp
matrix:
	include:
	- os: linux
	distro: trusty

install:
	- sudo apt-get install gcc-multilib
	- sudo apt-get install g++-multilib

script: make clean && make -f makefile32.mk && make -f makefile64.mk
